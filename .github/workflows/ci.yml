name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      package_desktop:
        description: Build an unpacked macOS desktop app for release-candidate validation
        required: false
        default: false
        type: boolean
      package_windows:
        description: Build and native-test Windows NSIS and portable executables
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  checks:
    name: Lint, build, and smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install root dependencies
        run: npm ci --no-audit --no-fund

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Install backend smoke dependencies
        run: python -m pip install fastapi pydantic python-multipart requests

      - name: Lint frontend
        run: npm run lint

      - name: Test clip draft workflow
        run: npm run test:clip-drafts --prefix frontend

      - name: Test transcript selection workflow
        run: npm run test:transcript-selection --prefix frontend

      - name: Test transcript search workflow
        run: npm run test:transcript-search --prefix frontend

      - name: Test speaker stats workflow
        run: npm run test:speaker-stats --prefix frontend

      - name: Test caption designer workflow
        run: npm run test:caption-designer --prefix frontend

      - name: Test social publishing workflow
        run: npm run test:social-publishing --prefix frontend

      - name: Test hook frame workflow
        run: npm run test:hook-frames --prefix frontend

      - name: Test support report redaction
        run: npm run test:support-report --prefix frontend

      - name: Test playback sync workflow
        run: npm run test:playback-sync --prefix frontend

      - name: Test long-media waveform workflow
        run: npm run test:waveform --prefix frontend

      - name: Test censorship workflow
        run: npm run test:censorship --prefix frontend

      - name: Build frontend
        run: npm run build --prefix frontend

      - name: Run backend smoke tests
        run: npm run smoke:backend

      - name: Compile backend
        run: python -m compileall -q backend

  package-desktop:
    name: Package macOS desktop app
    if: github.event_name == 'workflow_dispatch' && inputs.package_desktop
    runs-on: macos-14
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install root dependencies
        run: npm ci --no-audit --no-fund

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install backend dependencies
        run: npm run setup:backend

      - name: Install FFmpeg
        run: brew install ffmpeg

      - name: Prepare portable FFmpeg bundle
        run: npm run release:ffmpeg

      - name: Prepare standalone backend runtime
        run: npm run release:backend

      - name: Build and verify unpacked Apple Silicon desktop app
        run: |
          npm run dist:dir:arm64
          npm run check:packaged-ffmpeg -- --arch arm64

  checks-windows:
    name: Windows runtime checks
    runs-on: windows-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install root dependencies
        run: npm ci --no-audit --no-fund

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Install backend dependencies
        run: npm run setup:backend

      - name: Prepare checksum-verified Windows FFmpeg
        run: npm run release:ffmpeg:windows
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Run backend smoke tests on Windows
        run: npm run smoke:backend

      - name: Run desktop QA on Windows
        run: npm run qa:desktop

  package-windows:
    name: Package and native-test Windows desktop app
    if: github.event_name == 'workflow_dispatch' && inputs.package_windows
    runs-on: windows-latest
    timeout-minutes: 50

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install root dependencies
        run: npm ci --no-audit --no-fund

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Install backend dependencies
        run: npm run setup:backend

      - name: Build and verify Windows release
        run: npm run release:windows
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Upload Windows executables
        uses: actions/upload-artifact@v6
        with:
          name: ScriptCut-windows-x64
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/*.exe
            dist/release-windows/SHA256SUMS.txt
            dist/release-windows/release-manifest.json
